1: gitignore file make folder to get ignored while pushing.
 folder added node_modules (npm install ....)

 // We use ajax because every time creating a post our page refres so to avoid it we use ajax.